import styles from './Rodape.module.css';

export default function Rodape() {
    return(
        <footer className={styles.rodape}>
            <h2>Desenvolvido por Vinícius Roviello (via curso Alura).</h2>
        </footer>
    )
};